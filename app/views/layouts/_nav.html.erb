<div id='cssmenu'>
<ul>
    <% if user_signed_in? %>
    <li class='last'><%= link_to('Home', dashboards_path) %></li>
    <% else %>
    <li class='last'><%= link_to('Back', '/') %></a></li>
    <% end %>

    <% if user_signed_in? %>
    <li class='last'><%= link_to 'Profile', edit_user_registration_path %></li>
    <% end %>


    <% if user_signed_in? %>
    <li class='last'><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
    <% end %>

</ul>
</div>
